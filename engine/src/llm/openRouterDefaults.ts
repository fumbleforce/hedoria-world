/** Shared defaults (no store import — avoids circular deps). */
export const DEFAULT_OPENROUTER_TEXT_MODEL = "google/gemini-2.5-flash";
export const DEFAULT_OPENROUTER_IMAGE_MODEL = "google/gemini-2.5-flash-image";
